import { x as createFreeCamera } from "./index-C-tEgwbZ.js";
function parseBabylonCamera(cd) {
  var _a, _b;
  const p = cd.position;
  const pitch = ((_a = cd.rotation) == null ? void 0 : _a[0]) ?? 0;
  const yaw = ((_b = cd.rotation) == null ? void 0 : _b[1]) ?? 0;
  const cp = Math.cos(pitch);
  const cam = createFreeCamera({ x: p[0], y: p[1], z: p[2] }, { x: p[0] + cp * Math.sin(yaw), y: p[1] - Math.sin(pitch), z: p[2] + cp * Math.cos(yaw) });
  if (cd.fov != null) {
    cam.fov = cd.fov;
  }
  if (cd.minZ != null) {
    cam.nearPlane = cd.minZ;
  }
  if (cd.maxZ != null) {
    cam.farPlane = cd.maxZ;
  }
  return cam;
}
export {
  parseBabylonCamera
};
//# sourceMappingURL=parse-camera-CmZBS423.js.map
