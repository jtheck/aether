import { f as markMaterialUboDirty } from "./index-C-tEgwbZ.js";
function observableColor3(r, g, b, owner) {
  const arr = [r, g, b];
  for (let i = 0; i < 3; i++) {
    let val = arr[i];
    Object.defineProperty(arr, i, {
      get() {
        return val;
      },
      set(v) {
        if (val !== v) {
          val = v;
          markMaterialUboDirty(owner);
        }
      },
      configurable: true,
      enumerable: true
    });
  }
  return arr;
}
function observableVec2(x, y, owner) {
  const arr = [x, y];
  for (let i = 0; i < 2; i++) {
    let val = arr[i];
    Object.defineProperty(arr, i, {
      get() {
        return val;
      },
      set(v) {
        if (val !== v) {
          val = v;
          markMaterialUboDirty(owner);
        }
      },
      configurable: true,
      enumerable: true
    });
  }
  return arr;
}
function trackScalar(obj, key) {
  let val = obj[key];
  Object.defineProperty(obj, key, {
    get() {
      return val;
    },
    set(v) {
      if (val !== v) {
        val = v;
        markMaterialUboDirty(obj);
      }
    },
    configurable: true,
    enumerable: true
  });
}
function trackSubProps(parent, sub, keys) {
  for (const key of keys) {
    let val = sub[key];
    Object.defineProperty(sub, key, {
      get() {
        return val;
      },
      set(v) {
        if (val !== v) {
          val = v;
          markMaterialUboDirty(parent);
        }
      },
      configurable: true,
      enumerable: true
    });
  }
}
export {
  observableVec2 as a,
  trackSubProps as b,
  observableColor3 as o,
  trackScalar as t
};
//# sourceMappingURL=tracking-primitives-Ck5bgCuo.js.map
